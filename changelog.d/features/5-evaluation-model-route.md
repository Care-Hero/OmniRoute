- **feat(api):** new `POST /v1/evaluation-model` forwards the Vercel AI Gateway v4 evaluation contract (`vag/typesafe-ai/jev` — choice / score / boolean questions over a state string) with the stored `vercel-ai-gateway` credential, alias stripped to `ai-model-id`; evaluation models were listed under `vag/` but refused on chat completions ("is an evaluation model, not a language model") and had no route (Care-Hero fork)
