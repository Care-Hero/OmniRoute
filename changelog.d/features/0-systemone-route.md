- **feat(api):** new `POST /v1/systemone` serves TypeSafe's native System One contract (`{model, state, questions}` with `noul`/`choice`/`score`, answers with `confidence`, `usage.input_tokens`) through the Vercel AI Gateway evaluation route, so `@typesafe-ai/sdk` clients such as jev-axi and jevkit run through OmniRoute by setting `TYPESAFE_BASE_URL` — no TypeSafe key needed (Care-Hero fork)
